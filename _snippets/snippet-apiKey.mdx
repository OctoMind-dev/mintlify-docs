The option to create your API key can be found within the settings menu.

<Frame caption="Account settings menu icon, 5/2025">
  <img
    src="/images/run/go-to-account-settings.png"
    alt="Account settings menu icon"
  />
</Frame>

Click on `Create an API key`.

<Frame caption="Create an API key button, 05/2025">
  <img
    src="/images/run/create-API-key.png"
    alt="Link to create an API key in the Octomind app"
  />
</Frame>

You then will be navigated to our auth provider where you can create a new organization-level API key.

<Frame caption="Create a new Octomind API key in the settings, 05/2025">
  <img
    src="/images/run/API-keys-propelauth.png"
    alt="Create a new Octomind API key in the settings"
  />
</Frame>

<div class="mt-8" />

<Frame caption="Copy the Octomind API key">
  <img src="/images/run/API-key-created.png" alt="Copy the Octomind API key" />
</Frame>

<div class="mt-8" />

<Warning>
  You can only copy the API key once, so make sure to do that now. You should
  not share it with anyone.
</Warning>

Add the API key to your CI provider as a secret to run test reports from the CI.
